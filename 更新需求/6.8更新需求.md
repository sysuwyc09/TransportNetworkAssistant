# 6.8更新需求
代码重构需求：
将publicThread.py文件，按不同class的Qhread，放到不同的py文件中，保留原注释，py文件名与class名相同，放到thread文件夹中。
将publicFunc.py文件同时放到thread文件夹中。

修改main.py文件关于这两个文件的import语句


保留原publicThread.py文件备份，文件名publicThread_backup.py