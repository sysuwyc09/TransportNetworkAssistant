# 总体需求

新增一个一键更新数据库多个表格的功能模块

## 明细需求

## 一、main.py 文件updateOneKeyBtn 按钮触发事件，选择文件夹，作为需求表格的文件夹，并在publicThread.py新增一个QThread线程Class UpateOneKeyQThread，作为一键更新的功能入口

## 二、UpateOneKeyQThread功能细节：
一键更新数据库data\transportNetwork.db的以下表：
#数据库表清单
self.file_type_cols = [
    'OLT网元', '主光路', 'PON端口', '中继段', '光缆段', '站点', '机房', '光交箱', '分纤箱', 'ODF', '集群管理', '华为PON单板','中兴PON单板'
]
#数据库表列名及类型
self.file_cols = [
    (['网元名称', '所属机房', '设备型号', '设备IP','生命周期状态'],['str','str','str','str','str']),
    (['OLT名称', 'PON口', 'PON下挂ONU数量', 'OBD所属对象', '光路名称', '光路长度','光路文本路由'],['str','str','int','str','str','float','str']),
    (['所属传输网元', '端口名称', 'PONID', '端口状态', 'PON口下挂用户数', '端口子类型'],['str','str','str','str','int','str']),
    (['名称', '长度', '空闲数量', '占用数量', '中继纤芯数量', '始端站点', '终端站点', '始端机房','终端机房', '始端设施', '终端设施'],['str','float','int','int','int','str','str','str','str','str','str']),
    (['名称','所属光缆','实际长度','纤芯占用率','光纤数目','业务级别','敷设方式','维护部门','红线范围','初验时间','资源状态'],['str','str','float','float','int','str','str','str','str','str','str']),
    (['站点名称', '所属区县', '乡镇街道'],['str','str','str']),
    (['所属站点', '机房类型', '机房名称', '业务级别', '生命周期状态'],['str','str','str','str','str']),
    (['设施名称', '机房名称', '所属综合业务区','所属区县', '所属镇街','分纤点级别', '容量','经度','纬度'],['str','str','str','str','str','str','int','float','float']),
    (['设施名称', '机房名称', '所属综合业务区','所属区县', '所属镇街','分纤点级别', '容量','经度','纬度'],['str','str','str','str','str','str','int','float','float']),
    (['设施名称', '机房名称', '所属综合业务区','所属区县', '所属镇街','分纤点级别', '容量','经度','纬度'],['str','str','str','str','str','str','int','float','float']),
    (['设备名称','集群列表'],['str','str']),
    (['所属网元','槽位号','单板类型','单板状态'],['str','str','str','str']),
    (['网元名称','板卡槽位','板卡类型','板卡状态'],['str','str','str','str']),
]
（1）OLT网元：名称包含 "OLT设备" 的单个xlsx文件; 导入的表有以下2列需重命名为数据表的列名，其他列保持不变，需筛选数据库表的列，pandas读取时,header参数为0
- 所属机房：所属位置点/机房
- 设备IP：设备IP地址（省内系统：网管IP）

（2）主光路：名称包含 "主光路" 的单个xlsx文件; 数据库表的列名和 导入表格的列名全部对应，需筛选数据库表的列，pandas读取时,header参数为0

（3）PON端口：名称包含 "PON端口" 的多个xlsx文件; 数据库表的列名和 导入表格的列名全部对应，需筛选数据库表的列，pandas读取时,header参数为0

（4）中继段：名称包含 “中继段” 的单个csv文件; 数据库表的列名和 导入表格的列名全部对应，需筛选数据库表的列，读取需使用到publicFunc.py 的 readCsvFile函数，header为0

（5）光缆段：名称包含 "光缆段" 的多个csv文件; 数据库表的列名和 导入表格的列名全部对应，需筛选数据库表的列，读取需使用到publicFunc.py 的 readCsvFile函数，header为0

（6）站点：名称包含 "站点管理" 的单个xlsx文件;  数据库表的列名和 导入表格的列名全部对应，需筛选数据库表的列，pandas读取时,header参数为0

（7）机房：名称包含 "机房管理" 的单个xlsx文件;  数据库表的列名和 导入表格的列名全部对应，需筛选数据库表的列，pandas读取时,header参数为0

（8）光交箱：名称包含 "光交接箱" 的多个csv文件; 数据库表的列名和 导入表格的列名全部对应，需筛选数据库表的列，读取需使用到publicFunc.py 的 readCsvFile函数，header为0

（9）分纤箱：名称包含 "光分纤箱" 的多个csv文件; 数据库表的列名和 导入表格的列名全部对应，需筛选数据库表的列，读取需使用到publicFunc.py 的 readCsvFile函数，header为0

（10）ODF：名称包含 "ODF" 的多个csv文件; 数据库表的列名和 导入表格的列名全部对应，需筛选数据库表的列，读取需使用到publicFunc.py 的 readCsvFile函数，header为0

（11）集群管理：名称包含 "集群管理" 的单个xlsx文件;  数据库表的列名和 导入表格的列名全部对应，需筛选数据库表的列，pandas读取时,header参数为0

（12）华为PON单板：名称包含 "单板报表" 的单个xlsx文件;  数据库表的列名和 导入表格的列名全部对应，需筛选数据库表的列，pandas读取时,header参数为3

（13）中兴PON单板：名称包含 "card_query" 的单个xlsx文件;  数据库表的列名和 导入表格的列名全部对应，需筛选数据库表的列，pandas读取时,header参数为0

1、首先判断选择文件夹是否包含以上所有需求文件，如果不包含则提示用户添加缺失具体的文件，并结束线程。
2、按上面要求逐个读取文件，将数据写入数据库表中；